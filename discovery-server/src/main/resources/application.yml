spring:
  application:
    name: discovery-service
  profiles:
    active: development
  config:
    import: "optional:configserver:http://root:admin@localhost:8080"

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false

